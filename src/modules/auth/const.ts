export const WALLET_CONNECT_DIALOG_ID = 'wallet-connect-dialog';

/**
 * Cache key for wallet connection. Used to select connection data from the store.
 */
export const CONNECTION_CACHE_KEY = 'wallet-connection';

/**
 * The name of the parameter in the address bar, which stores the wallet id
 * for connecting to the wallet via deep linking.
 */
export const WALLET_PARAM = 'wallet';
